{
  "name": "csv-objects",
  "version": "2.0.1",
  "description": "Library to convert CSV files to JSON objects, with support for nested arrays and objects",
  "main": "src/csv-objects.js",
  "scripts": {
    "test": "jasmine-node spec --captureExceptions",
    "build": ""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "async": "^1.5.2",
    "csv": "^0.4.6"
  },
  "devDependencies": {
    "jasmine-node": "^1.14.5"
  }
}
